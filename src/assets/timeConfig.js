export const DEF_MINUTES = 5;
export const DEF_SECONDS = 30;

export const MAX_MINUTES = 99;
export const MAX_SECONDS = 59;

export const DEF_TIME = {
    minutes: DEF_MINUTES,
    seconds: DEF_SECONDS
};